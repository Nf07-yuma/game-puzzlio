# Copy this file to android/key.properties (which is gitignored) for local
# release builds, and fill in the values from your own upload keystore.
# See README.md "Signing release builds" for how to generate one.
storePassword=your-store-password
keyPassword=your-key-password
keyAlias=upload
storeFile=upload-keystore.jks
